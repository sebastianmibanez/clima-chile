datos/
node_modules/
estaciones-dmc.json
.env
gatitos/*.eps
gatitos/*.jpg
*Zone.Identifier

# Instalador de USB booteable, nada que ver con el proyecto
ventoy-*.zip

# El .zip del canvas: su contenido vive extraido en diseno/rediseno-movil/
Rediseño*.zip
